

class Label_Distance():

    def __int__(self):
        pass

    def assign_label(self, matrizDist, labels, k):
        k=k+1
        vect=[]
        wid = len(matrizDist)
        heigh = len(matrizDist[0])
        
        vect=[0 for i in range(k)]
        for height in range (0, heigh):
                    for width in range (0, wid):
                          #if label tal
                           # vect[] +=
                        

